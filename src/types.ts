interface CubeColorType {
  ceil: string;
  side1: string;
  side2: string;
}

interface GardenInfoType {
  date: number;
  week: number;
  count: number;
  id: number;
}

export type { CubeColorType, GardenInfoType };
